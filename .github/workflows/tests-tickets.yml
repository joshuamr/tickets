name: tests-tickets
on: 
  pull_request
  
jobs: 
  tickets-test:
    name: tickets-test
    runs-on: ubuntu-latest
    steps: 
      - name: Get code
        uses: actions/checkout@v2
      - name: Cache dependencies
        uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: 'npm'
          cache-dependency-path: tickets/package-lock.json
      - name: Change into tickets dir
        run: cd tickets 
      - name: Install dependencies
        run: npm install 
      - name: Run tests
        run: npm run test:ci
